# Stage 08 - Shared Prompt Refinement And Model Evaluation

## Agreement And Scope

Scope `prompts-1`, authorized on 2026-09-16 under plan 1.4. The user requested execution
and automatic merge of stage 08, followed by stage 09 and its automatic merge, then a final
report and stop for review on a new repository. This is advance scoped delivery authority,
not a later manual acceptance or permission to skip verification. No new merge question is
needed for these two stages. Closing documents must be committed before each merge.

The previously accepted solo-execution exception continues. Self-review is not independent
review. No subagent/model-switch tool is available here; observed model/effort and usage are
unknown. No paid API, runtime installation, provider write or real adopter migration is allowed
by this scope. The future review on a new repository remains separate.

Base: accepted 0.4.0 at `a60dca9638bf12cd84244aedcf5fc8d2a9734baa`.
Branch: `codex/refactor-08-prompts`. Preserve that baseline for 08/09 comparisons.
Assess every research lead, refine one shared model-neutral instruction set, and check its
meaning, navigation and usable adoption. Promote no cross-model or cost claim without actual
measurements. Lack of another model is disclosed, not simulated as success.

## Source Assessment And Decisions

All 15 registered sources were attempted before editing reusable instructions: nine readable,
six unavailable. [Research assessment](../research-08.md) records individual evidence, the
separate supplemental official article and model/runtime limitations. Source descriptions and
URLs remain historical leads; unavailable posts are not quoted as verified facts.

Main changes and owners:

- Review/E2E entrypoints: concise activation and one conditional mode router, preserving
  target-owned policy, no-tracker use and independent-review requirements.
- Common authorization and E2E runbook: advance explicit merge authority remains valid for
  its scope after current evidence/head checks. No implicit merge or unconditional approval.
- GitHub review mode: use its selected read-only connection, without imposing Azure MCP actor
  rules on GitHub. ADO/Jira remain MCP-only with no alternate command or raw-API path.
- Review privacy: protect private prompts/payloads while allowing approved public/synthetic
  instruction excerpts to be reviewed.
- Documentation and runtime-route owners: preserve one shared set, compare actual behavior,
  keep model selection separate and distinguish unavailable evidence from evaluated settings.

No per-model prompt edition, schema change, new runner, source optimizer or central reporting
service. Tuned model/effort recommendations are not promoted: inherit remains a compatibility
default, and existing explicitly authorized runtime routes remain usable.

## Evaluation And Verification

[Prompt evaluation](../prompt-evaluation-08.md) records baseline/candidate component hashes,
whitespace-word proxies and ten semantic scenarios. They are author-reviewed contracts, not
an agent pass rate. No host activation, other-model execution, independent reproduction or
measured model cost was possible. Documentary model identifiers do not prove account access.

Local focused validation passed for the changed documents and three affected skills. The
selected actual-standard-copy adoption case passed: one executed test, zero skipped/cancelled.
Whitespace checks also passed. Full repository checks remain in CI. The one fresh-adoption local case uses actual source standards and native assertions;
passing it does not establish instruction-following or live integration correctness.

## Authorized Closeout

The user pre-authorized completion and merge of this scoped delivery. The local closing
commit includes STATUS, this card and the 0.4.1 changelog entry before publication. Semantic
review found no remaining actionable issue in the changed scope. This does not claim a
multi-model benchmark: candidate route execution and real cost/latency comparisons remain
unavailable and are carried into the final post-09 review. Required final-head CI must pass
before merge; exact source/tested/merge evidence belongs in the same PR.

## Delivery And Closeout

A branch-only read-only workflow transferred the pinned base archive because direct Git
network access is unavailable. The base tree and commit object were reconstructed and matched
Git. That workflow is removed from the final tree and is absent from the PR diff. Local edits
and commits are published using GitHub Git objects on the normal remote parent chain; no force
push, history rewrite or credentials export. Local and remote content trees are compared.

Under the user's advance authorization, prepare closing STATUS/card/version notes on this
branch, run focused checks, commit locally and publish, then verify final-head CI and merge
only that head. Prepared Done in an open PR is not observed merge. Final source, tested and
merge revisions belong in the PR, not a self-referential documentation commit. Stage 09 may
start only after confirmed merge; no stage 10 or new-repository operation is authorized.

## Navigation

- [Current state](../STATUS.md)
- [Plan](../../refactor-plan.md#step-08-shared-prompt-refinement-and-model-evaluation)
- [Research](../research-08.md)
- [Evaluation](../prompt-evaluation-08.md)
- [Documentation index](../../INDEX.md)
