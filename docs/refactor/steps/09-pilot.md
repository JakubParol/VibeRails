# Stage 09 - Bounded Pilot And Comparison

## Agreement And Scope

Scope `pilot-1`, 2026-09-16, under plan 1.4 and the user's explicit advance instruction:
execute/merge 08, then execute/merge 09, report and STOP for review on a new repository.
Stage 08 is confirmed merged in PR #15 at `d92f6fa34d1809997c2d9629d81ca382d00e9ff2`.
Branch: `codex/refactor-09-pilot`. Earlier evidence and baseline 0.4.0 remain unchanged.
Solo execution/self-review is the user-approved exception; no independent agent is claimed.

Compare frozen 0.4.0 (`a60dca9638bf12cd84244aedcf5fc8d2a9734baa`) with 0.4.1 above on
isolated small and multi-root Python projects. Use the same task inputs and code correction
for each version, actual source standards/audit tools, real native tests, Git commits and
clean-clone handoff. Exercise a protected local customization during an actual version refresh.
No external adopter or account is modified. No paid model run, installation or provider write.

Acceptance: native behavior and architecture checks reject seeded defects, then pass after
the scoped correction; adopted documents audit correctly under both versions; local settings,
warnings and required-check declarations survive refresh; drift is rejected before explicit
repinning; a clean local clone reproduces checks; stale handoff evidence is not relabeled.
Record failures and corrections, elapsed script/check times separately from unavailable agent
usage, and distinguish deterministic compatibility from model-performance or team-productivity
proof. No independently dispatchable models or second developer are available here.

This is a controlled artifact/workflow pilot, not a live organizational rollout or blinded
LLM A/B experiment. Cross-model efficiency, real team handoff and provider enforcement remain
unproved for the post-09 new-repository review. Passing fixtures must not hide those gaps.

## Executed Evidence

The [pilot report](../pilot-09.md) records actual pinned inputs, six scenarios, results and
limitations. Native Python checks execute 11 cases in the small project and 15 in the multi-root
project. Both versions pass after the same correction and in clean local clones. The real
source refresh previews four changed files through Git, retains 18 old copy refs and preserves
local settings/warnings. Drift and version-only rollback fail; reviewed matching state passes.
A later regressed task commit invalidates an old clean clone's evidence.

First local execution: two scenarios passed and four stopped at an incorrect author-written
assertion about the audit disclaimer. A narrow expectation fix made all four affected scenarios
pass. A stronger native skip-summary guard passed its selected check; a separate negative probe
rejected an exit-zero run with three skipped cases. No failed test was hidden,
no pack rule weakened and no unchanged full repository suite rerun locally. Full final-head CI
must execute all six pilot cases alongside the existing 28 cases and architecture examples.

Single-agent review distinguishes deterministic fixture actions from live agent choices.
No new runtime library, migration engine, model API or external project change was introduced.
The tests and CI are the only executable additions. Actual local commits exist; remote publication
uses connected GitHub Git objects because Git network transport is unavailable. The reviewed
local tree must match the published tree, with normal ancestry and no force push.

## Authorized Closeout And Stop

The user explicitly pre-authorized this stage's execution and automatic merge after 08, then
requested a report and stop. Closing STATUS/card changes are prepared locally and committed
before publication and final-head CI. No new acceptance message is claimed. The Done record
in an open PR is prepared only; completion requires confirmed merge and remote readback.
Final source/tested/merge evidence belongs in the stage PR, not a self-referential commit.

The controlled scope is complete subject to that CI/merge confirmation. Real-repository task
trials, independent developer handoff, model/effort and cost comparisons, live MCP permissions,
and platform coverage remain unproved as listed in the report. They are next-review inputs,
not silently passed acceptance criteria. No tuned route or production-readiness claim is made.

After merge: STOP for the user's new-repository review and continuation decision. Do not create
stage 10's branch/card, run telemetry work or modify a new repository. This is the explicit
post-09 decision point, not automatic approval of later stages.

## Delivery

Preserve current CI and add only the bounded pilot verification. Locally run the new pilot
and changed-file checks, not the aggregate repository gate. Before automatic merge, update
closing STATUS/card locally, commit and publish this same branch, check actual final-head CI,
merge with expected-head protection and read back remote main. A prepared Done row is not a
completed merge while the PR is open. After confirmed merge provide the final report and stop.
Stage 10 and new-repository operations are not authorized.

## Navigation

- [Current state](../STATUS.md)
- [Pilot contract](../../refactor-plan.md#step-09-pilot-and-comparison)
- [Stage 08 evidence](08-prompts.md)
- [Documentation index](../../INDEX.md)
